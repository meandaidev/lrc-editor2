1. * ปุ่มกระโดด [✅ แก้แล้ว]
2. * ถัดจากปุ่ม "Current Time:" ให้แสดงเนื้อเพลงปัจจุบัน [❌ ยกเลิก]
3. * ตอนกดเพิ่มบันทัดให้ Pause ไว้ก่อน ปิดหน้าต่างค่อย Play ต่อ [✅ แก้แล้ว]
4. * อยากให้มี Loader แสดงตอนกำลังโหลดเพลง พอโหลดเสร็จค่อยเปิด Modal หน้าให้วางเนื้อเพลง [✅ แก้แล้ว]
5. * เอาปุ่ม เพิ่มเนื้อเพลง ที่อยู่ที่ Header ออก [✅ แก้แล้ว]
6. * ช่วยเพิ่มให้มี Footer เล็กๆ [✅ แก้แล้ว]
7. * ตอนหน้าต่าง Modal วางเนื้องเพลงโชว์ขึ้นให้ Focus ที่ text area เพื่อจะได้พร้อมพิมพ์เลย [✅ แก้แล้ว]
8. * แบบไฟล์เดียวโหลดเสร็จใส่เนื้องเพลงแล้วแต่กดเริ่มเล่นเพลงไม่ได้ [✅ แก้แล้ว]
9. * หน้าใส่เนื้อเพลง ขอเปลี่ยนชื่อปุ่มจาก ยกเลิก เป็น ปิดหน้าต่าง [✅ แก้แล้ว]
10. * แก้ bug: กดปุ่ม Play แล้วเพลงไม่เล่น - ReferenceError: audioSource is not defined [✅ แก้แล้ว]
11. * แก้ bug: กดปุ่ม "เพิ่มบันทัดล่าง" เพลงไม่หยุดรอ (audioRef ไม่พร้อม) [✅ แก้แล้ว]
12. * แก้ปัญหา: คลิกแถวในตาราง lyrics ทำงานเหมือนกดปุ่ม Jump [✅ แก้แล้ว]
13. * หน้าต่างใส่เนื้อเพลงยังไม่ Responsive [✅ แก้แล้ว]
14. * หน้าต่างข้อมูลเพลงยังไม่ Responsive [✅ แก้แล้ว]
15. * ช่วยเปลี่ยนไอคอนเฟืองเป็นไอคอนที่สือถึงข้อมูล ข้อมูลเพลง [✅ แก้แล้ว]
16. * แก้ปัญหาความสูง modal ข้อมูลเพลง [✅ แก้แล้ว]
17. * แก้ปัญหาความสูง modal ใส่เนื้อเพลง [✅ แก้แล้ว]
17. * แก้ปัญหาความสูง modal ใส่เนื้อเพลง [✅ แก้แล้ว]
18. * ช่วยจัดเรียงปรับลดความสูงส่วน Player control หน่อย (ใช้พื้นที่ 3 บันทัดก็พอ) [✅ แก้แล้ว]
19. * ช่วยจัดเรียงปรับลดความสูงส่วน Player control หน่อย (ใช้พื้นที่ 2 บันทัดก็พอ) [✅ แก้แล้ว]
20. * Player control 2 บันทัดกำลังพอดีเลย แต่ฉันว่าคุณจัดใหม่ได้ลงตัวกว่านี้ลองดู [✅ แก้แล้ว]
21. * ปุ่ม "+ Add Line at Buttom" ตอนนี้เด่นใหญ่กว่าเพื่อนเลย ปรับให้พอดีหน่อย [✅ แก้แล้ว]
22. * ตอนกดสลับ Mix Inst Vocal ให้เริ่มเล่นเลยได้ไหมเพราะสัญลักบนปุ่มก็เป็น Paused อยู่ [✅ แก้แล้ว]
23. * ตอนกดสลับ Mix Inst Vocal ให้จำตำแหน่งการเล่นล่าสุดไว้แล้วเล่นต่อที่จุดเดิมเลยได้ไหม [❌ ยกเลิก]
24. * ช่วยทบทวน Flow การทำงานและปรับปรุง เพราะผู้ใช้จะมี 2 แบบ แบบแรกคือเตรียมเนื้องร้องทั้งหมดมาวางในหน้า "Enter Lyrics" แล้วไปต่อขั้นต่อไปซึ่งทำงานเหมือนแบบที่สองเลย กับแบบที่สองที่ไม่มีเนื้อเพลงมาตั้งใจจะข้ามหน้าต่างนี้โดยการกดปุ่ม "Close Windows" แสดงว่าตั้งใจจะมาใส่เนื้อเพลงโดยการกดปุ่ม "+Add Line at Buttom" ไปทีล่ะบันทัดจนจบเพลง(จำเป็นจะต้องแก้ไข ถ้า Close) [OK]
25. * ปรับปุ่มเปิด Song Metadata ให้ใหญ่ขึ้นกว่านี้ [OK]
26. * ช่วยสลับตำแหน่งปุ่ม Inst กับ Volcal [OK]
27. * มีการกดบันทึกเวลาจบได้ด้วย แต่ตอน Export ขาดเวลาจบ รูปแบบ [startTime-endTime] [OK]
28. * ช่วยเอาปุ่ม Import LRC ออก [OK]
29. * ที่หน้าแรกถ้ามีการเลือกไฟล์ LRC ที่มีเวลาจบเข้ามาด้วย จะโหลดข้อมูลเข้าตารางเนื้อร้องได้ไม่ถูกต้อง มาเพียงบันทัดเดียว []
30. * ควรมีสถานะว่า Player พร้อมเล่น จะได้เริ่มกดเพื่อทำขั้นต่อไปต่อได้ []
31. * กด "+ Add Line at Buttom" เพลงหยุดหลังจากเสร็จแล้วไปกดปุ่ม Jump เพลงจะกลับไปเล่นเริ่มต้นใหม่ ที่ถูกต้องต้องเริ่มเล่นตามเวลาเริ่มของรายการเนื้อเพลงบันทัดนั้นที่ถูกกด []

## บันทึกการแก้ไขล่าสุด:

### รายการ 11: แก้ bug การ pause/resume เสียงเมื่อเปิด modal
- **ปัญหา**: กดปุ่ม "เพิ่มบันทัดล่าง" เพลงไม่หยุดเพราะ audioRef ยังไม่พร้อม
- **สาเหตุ**: LyricsTable render ก่อนที่ AudioPlayer จะ set audioRef ใน store
- **วิธีแก้**: เพิ่ม waitForAudioRef function ที่รอ audioRef พร้อมก่อน pause (ลองทุก 50ms สูงสุด 10 ครั้ง)
- **ไฟล์ที่แก้**: `src/components/LyricsTable.jsx`

### รายการ 12: แก้ปัญหาการคลิกแถวในตาราง
- **ปัญหา**: คลิกพื้นที่ว่างในแถวของ lyrics table ทำงานเหมือนกดปุ่ม Jump
- **สาเหตุ**: table row มี onClick event ที่เรียก playFromLyric() ซึ่งทำงานเหมือน jumpToLine()
- **วิธีแก้**: ลบ onClick event และ cursor-pointer class ออกจาก table row, ลบ playFromLyric จาก import
- **ไฟล์ที่แก้**: `src/components/LyricsTable.jsx`

### รายการ 13: ทำหน้าต่างใส่เนื้อเพลงให้ Responsive
- **ปัญหา**: AddLyricModal ไม่ responsive บนหน้าจอเล็ก
- **วิธีแก้**: 
  - เพิ่ม responsive breakpoints (xs, sm, md, lg)
  - ปรับขนาด modal ตามหน้าจอ: max-w-xs sm:max-w-md md:max-w-lg lg:max-w-xl
  - ปรับ padding/margin: mx-2 sm:mx-4, p-2 sm:p-3
  - ปรับขนาด text และ icons: text-lg sm:text-xl, size={20}
  - เปลี่ยน layout buttons เป็น flex-col sm:flex-row
- **ไฟล์ที่แก้**: `src/components/AddLyricModal.jsx`

### รายการ 14: ทำหน้าต่างข้อมูลเพลงให้ Responsive
- **ปัญหา**: MetadataModal ไม่ responsive บนหน้าจอเล็ก
- **วิธีแก้**: 
  - เพิ่ม responsive breakpoints เช่นเดียวกับ AddLyricModal
  - เพิ่ม max-height และ overflow-y-auto สำหรับหน้าจอเล็ก
  - ปรับ spacing: space-y-3 sm:space-y-4
  - ปรับขนาด labels และ inputs: text-xs sm:text-sm
  - เปลี่ยน layout buttons เป็น flex-col sm:flex-row
- **ไฟล์ที่แก้**: `src/components/MetadataModal.jsx`

### รายการ 15: เปลี่ยนไอคอนเฟืองเป็นไอคอนข้อมูล
- **ปัญหา**: ไอคอน Settings (เฟือง) ไม่สื่อความหมายเรื่องข้อมูลเพลงชัดเจน
- **วิธีแก้**: เปลี่ยนจาก Settings เป็น Info ไอคอน
- **ไฟล์ที่แก้**: 
  - `src/components/AudioPlayer.jsx` - import และปุ่มเปิด modal
  - `src/components/MetadataModal.jsx` - header ของ modal
- **หมายเหตุ**: ไอคอน Info จะสื่อความหมายเรื่องข้อมูลได้ดีกว่า

### รายการ 16: แก้ปัญหาความสูง modal ข้อมูลเพลง
- **ปัญหา**: MetadataModal มีความสูงมากเกินไป ทำให้เนื้อหาล้นออกจากหน้าจอ
- **สาเหตุ**: ใช้ `max-h-96 sm:max-h-none` ทำให้บนหน้าจอใหญ่ไม่มีการจำกัดความสูง
- **วิธีแก้**: 
  - เปลี่ยนเป็น `max-h-[90vh]` เพื่อจำกัดความสูงไม่เกิน 90% ของ viewport height
  - ใช้ `flex flex-col` structure: header (flex-shrink-0), content (flex-1 overflow-y-auto), footer (flex-shrink-0)
  - เพิ่ม `min-h-0` เพื่อให้ flex item สามารถ shrink ได้
- **ไฟล์ที่แก้**: `src/components/MetadataModal.jsx`
- **ผลลัพธ์**: modal จะมีความสูงที่เหมาะสมกับหน้าจอ และมี scroll bar เมื่อเนื้อหาเยอะ

### รายการ 17: แก้ปัญหาความสูง modal ใส่เนื้อเพลง
- **ปัญหา**: LyricsInput modal มีความสูงมากเกินไป ทำให้เนื้อหาล้นออกจากหน้าจอ
- **สาเหตุ**: ไม่มีการจำกัดความสูงและไม่ responsive
- **วิธีแก้**: 
  - เพิ่ม `max-h-[90vh]` เพื่อจำกัดความสูงไม่เกิน 90% ของ viewport height
  - ใช้ `flex flex-col` structure: header (flex-shrink-0), content (flex-1 overflow-y-auto), footer (flex-shrink-0)
  - เพิ่ม responsive breakpoints: max-w-sm sm:max-w-lg md:max-w-xl lg:max-w-2xl
  - ปรับขนาด textarea: h-48 sm:h-64, text-xs sm:text-sm
  - ปรับ padding, spacing ตามขนาดหน้าจอ
  - เปลี่ยน layout buttons เป็น flex-col sm:flex-row
- **ไฟล์ที่แก้**: `src/components/LyricsInput.jsx`
- **ผลลัพธ์**: modal มีความสูงเหมาะสม responsive และใช้งานได้ดีบนทุกขนาดหน้าจอ

### รายการ 18: ปรับลดความสูงส่วน Player control
- **ปัญหา**: AudioPlayer component ใช้พื้นที่ความสูงมากเกินไป (มากกว่า 3 บรรทัด)
- **เป้าหมาย**: ลดความสูงให้เหลือแค่ 3 บรรทัดเท่านั้น
- **วิธีแก้**: 
  - **แถว 1**: Audio Type Selector (เฉพาะเมื่อมีไฟล์หลายไฟล์) - ลดขนาด button และ text
  - **แถว 2**: Progress bar พร้อม time display แบบ inline (เวลาอยู่ซ้าย-ขวาของ progress bar)
  - **แถว 3**: Controls และ Audio info ในแถวเดียวกัน (controls ซ้าย, info ขวา)
  - ลด padding จาก `p-4` เป็น `p-3`
  - ลด margin ระหว่างส่วนต่างๆ จาก `mb-4` เป็น `mb-2`
  - ลดขนาด icons และ buttons
  - ปรับ volume slider ให้เล็กลง
- **ไฟล์ที่แก้**: `src/components/AudioPlayer.jsx`
- **ผลลัพธ์**: Player control ใช้พื้นที่แค่ 3 บรรทัด กะทัดรัดและประหยัดพื้นที่

### รายการ 19: ปรับลดความสูงส่วน Player control เป็น 2 บรรทัด
- **ปัญหา**: รายการ 18 ลดเหลือ 3 บรรทัด แต่ยังต้องการให้กะทัดรัดกว่านี้
- **เป้าหมาย**: ลดความสูงให้เหลือแค่ 2 บรรทัดเท่านั้น
- **วิธีแก้**: 
  - **แถว 1**: รวม Audio Type Selector + Progress bar + Time display ในแถวเดียวกัน
    - Audio Type Selector อยู่ซ้ายสุด (ย่อข้อความ: Mixed→Mix, Instrumental→Inst)
    - Time display + Progress bar + Duration ต่อกันในแถวเดียว
    - ลดขนาด button และ icons เพิ่มเติม (size 12-14px)
  - **แถว 2**: Controls + Audio info (เหมือนเดิม แต่ลดขนาดลง)
    - ลดขนาด icons controls เป็น 16-18px
    - ลด volume slider ให้เล็กกว่าเดิม (w-12)
    - ลด padding จาก `p-3` เป็น `p-2`
    - ลด margin จาก `mb-2` เป็น `mb-1`
- **ไฟล์ที่แก้**: `src/components/AudioPlayer.jsx`
- **ผลลัพธ์**: Player control ใช้พื้นที่แค่ 2 บรรทัด ประหยัดพื้นที่มากขึ้น

### รายการ 20: ปรับ Player control 2 บรรทัดให้ "ลงตัวกว่านี้"
- **ปัญหา**: Player control 2 บรรทัดใช้งานได้แต่ยัง layout/spacing/alignment ยังไม่ลงตัว
- **เป้าหมาย**: ปรับปรุง design และ visual balance ให้ดูสวยและสมดุลกว่าเดิม
- **วิธีแก้**: 
  - **แถว 1**: ปรับ spacing จาก `space-x-3` เป็น `gap-3` และปรับปรุง Audio Type Selector
    - เปลี่ยน rounded จาก `rounded-lg` เป็น `rounded-md` ให้ดูเรียบ
    - เพิ่ม `shadow-sm` และ hover effects ที่ดีกว่า (`hover:bg-gray-200`)
    - ปรับ Time display ให้มี background (`bg-gray-50`) และ center align
    - เพิ่ม `font-mono` สำหรับ time display เพื่อความสม่ำเสมอ
    - ปรับ Progress bar เพิ่ม `mx-1` เพื่อ spacing ที่ดีกว่า
  - **แถว 2**: ปรับปรุง Controls และ Audio Info
    - เปลี่ยน spacing จาก `space-x-2` เป็น `gap-2`
    - เพิ่ม hover effects (`hover:bg-blue-50`) สำหรับปุ่ม
    - ปรับ Volume slider ให้มี background (`bg-gray-50`) และขยายขนาดเป็น `w-14`
    - ปรับ Audio Info ให้มี background (`bg-gray-50`) และ style ที่ดีกว่า
    - เพิ่ม `shadow-sm` และ `hover:shadow-md` สำหรับ Play button
- **ไฟล์ที่แก้**: `src/components/AudioPlayer.jsx`
- **ผลลัพธ์**: Player control 2 บรรทัดที่มี visual balance ดีกว่า มี hover effects และ spacing ที่ลงตัว

### รายการ 21: ปรับปุ่ม "+ Add Line at Bottom" ไม่ให้เด่นใหญ่เกินไป
- **ปัญหา**: ปุ่ม "+ Add Line at Bottom" ใช้ class `btn-primary` ทำให้ดูเด่นเกินไปเมื่อเทียบกับ UI อื่นๆ
- **เป้าหมาย**: ปรับให้ปุ่มมีขนาดและสีที่พอดีกับ UI ทั่วไป ไม่เด่นเกินไป
- **วิธีแก้**: 
  - เปลี่ยนจาก `btn-primary` (สีน้ำเงินเด่น) เป็น secondary button style
  - ใช้ `bg-gray-100 border border-gray-300 text-gray-700` แทน
  - ลดขนาด icon จาก `size={16}` เป็น `size={14}`
  - ปรับ spacing จาก `space-x-2` เป็น `gap-1.5`
  - เปลี่ยน text size เป็น `text-sm`
  - เพิ่ม hover states: `hover:bg-gray-200 hover:text-gray-900`
  - เพิ่ม focus states สำหรับ accessibility
  - ใช้ `inline-flex` แทน `flex` เพื่อ sizing ที่ดีกว่า
- **ไฟล์ที่แก้**: `src/components/LyricsTable.jsx` (ปุ่มทั้งบนและล่าง)
- **ผลลัพธ์**: ปุ่ม "+ Add Line at Bottom" มีขนาดและสีที่พอดี ไม่เด่นเกินไป กลมกลืนกับ UI อื่นๆ